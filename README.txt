To get the code running, do the following:
1. Run the InitData.sql script against localhost master to create the DBs and populate with seed data.
2. Run: npm install on client app.
3. Run: Set API, STS, and Client app, all three as startup project and start running.
4. go to https://localhost:4201